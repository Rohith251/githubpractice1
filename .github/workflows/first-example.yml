name: My first workflow

on: push

jobs:
    first_job:
       runs-on: ubuntu-latest
      #  container:
      #   image: oraclelinux:8
       steps:
            - name: checkout
              uses: actions/checkout@v4
            
            - name: welcome
              run: 
                   maven --version
                   node --version

            - name: first
              run: echo "well"
              
            - name: list files
              run: ls

            - name: readme
              run: cat README.md

